# 📊 نظام الإنفوجرافيك - دليل الاستخدام الكامل

## 🎯 نظرة عامة

تم تطوير نظام إنفوجرافيك متكامل لموقع غرفة الأخبار يتيح عرض وإدارة الإنفوجرافيكات بشكل احترافي مع دعم كامل للغتين العربية والإنجليزية.

---

## ✨ المميزات الرئيسية

### 🎨 الفرونت إند (Frontend)
- ✅ مكون Vue.js احترافي للعرض (`InfographicSection.vue`)
- ✅ تصميم متجاوب (Responsive) يدعم جميع أحجام الشاشات
- ✅ عرض شبكي جذاب (1-3 أعمدة حسب حجم الشاشة)
- ✅ نافذة منبثقة (Lightbox) لعرض الإنفوجرافيك بالحجم الكامل
- ✅ تأثيرات hover احترافية
- ✅ دعم الوضع الليلي (Dark Mode)
- ✅ رسوم متحركة سلسة
- ✅ تحميل البيانات ديناميكياً من API

### 🔧 الباك إند (Backend)
- ✅ موديل Laravel كامل مع Relations
- ✅ Migration لقاعدة البيانات مع Indexes محسّنة
- ✅ Admin Controller للإدارة الكاملة (CRUD)
- ✅ API Controller مع Endpoints متعددة
- ✅ صفحات Blade احترافية للوحة التحكم
- ✅ رفع وإدارة الصور
- ✅ نظام Tags والفلترة
- ✅ دعم Featured و Active Status
- ✅ حساب المشاهدات
- ✅ ترجمة ثنائية اللغة

---

## 📁 بنية الملفات

### Frontend
```
frontend/
├── components/
│   ├── DynamicSection.vue (محدث)
│   └── sections/
│       └── InfographicSection.vue (جديد)
```

### Backend
```
backend/
├── app/
│   ├── Models/
│   │   └── Infographic.php (جديد)
│   ├── Http/
│   │   └── Controllers/
│   │       ├── Admin/
│   │       │   └── InfographicController.php (جديد)
│   │       └── Api/
│   │           └── InfographicController.php (جديد)
├── database/
│   ├── migrations/
│   │   └── 2025_11_21_162629_create_infographics_table.php (جديد)
│   └── seeders/
│       └── InfographicSeeder.php (جديد)
├── resources/
│   └── views/
│       └── admin/
│           └── infographics/
│               ├── index.blade.php (جديد)
│               ├── create.blade.php (جديد)
│               └── edit.blade.php (جديد)
└── routes/
    ├── api.php (محدث)
    └── admin.php (محدث)
```

---

## 🚀 خطوات التثبيت

### 1. تشغيل Migration

```bash
cd backend
php artisan migrate
```

### 2. (اختياري) تشغيل Seeder لإضافة بيانات تجريبية

```bash
php artisan db:seed --class=InfographicSeeder
```

**ملاحظة:** ستحتاج إلى إضافة صور تجريبية في المجلد `storage/app/public/infographics/`

### 3. إنشاء Symbolic Link للـ Storage (إذا لم يكن موجوداً)

```bash
php artisan storage:link
```

---

## 📊 هيكل قاعدة البيانات

### جدول `infographics`

| العمود | النوع | الوصف |
|--------|------|-------|
| id | BIGINT | المعرف الفريد |
| title | VARCHAR(255) | العنوان بالعربية |
| title_en | VARCHAR(255) | العنوان بالإنجليزية |
| description | TEXT | الوصف بالعربية |
| description_en | TEXT | الوصف بالإنجليزية |
| image | VARCHAR(255) | مسار الصورة |
| slug | VARCHAR(255) | رابط SEO-friendly |
| category_id | BIGINT | معرف القسم (اختياري) |
| is_featured | BOOLEAN | مميز؟ |
| is_active | BOOLEAN | نشط؟ |
| views | INTEGER | عدد المشاهدات |
| order | INTEGER | ترتيب العرض |
| tags | JSON | الوسوم |
| created_at | TIMESTAMP | تاريخ الإنشاء |
| updated_at | TIMESTAMP | تاريخ التحديث |

**Indexes:**
- `[is_active, order]` - للفلترة السريعة
- `category_id` - للبحث حسب القسم
- `created_at` - للترتيب الزمني

---

## 🔌 API Endpoints

### Public Endpoints

#### 1. قائمة الإنفوجرافيكات
```
GET /api/v1/infographics
```

**المعاملات (Query Parameters):**
- `limit` (int): عدد النتائج (افتراضي: 12، الحد الأقصى: 50)
- `category_id` (int): الفلترة حسب القسم
- `featured` (bool): عرض المميزة فقط
- `search` (string): البحث في العنوان والوصف

**الاستجابة:**
```json
{
  "data": [
    {
      "id": 1,
      "title": "إنفوجرافيك الانتقلي السياسي 2024",
      "title_en": "Political Transitional Infographic 2024",
      "image": "infographics/sample1.jpg",
      "category": {...},
      "views": 150,
      ...
    }
  ],
  "meta": {
    "current_page": 1,
    "last_page": 3,
    "per_page": 12,
    "total": 35
  }
}
```

#### 2. إنفوجرافيك واحد
```
GET /api/v1/infographics/{slug}
```

#### 3. الإنفوجرافيكات المميزة
```
GET /api/v1/infographics/featured
```

#### 4. آخر الإنفوجرافيكات
```
GET /api/v1/infographics/latest?limit=6
```

#### 5. الأكثر مشاهدة
```
GET /api/v1/infographics/popular?limit=6
```

#### 6. إنفوجرافيكات ذات صلة
```
GET /api/v1/infographics/{slug}/related
```

---

## 🎨 استخدام المكون في الفرونت إند

### 1. إضافة قسم إنفوجرافيك في الصفحة الرئيسية

من لوحة التحكم:
```
1. انتقل إلى: أقسام الصفحة الرئيسية → إضافة قسم جديد
2. املأ البيانات:
   - اسم القسم: إنفوجرافيك
   - نوع القسم: إنفوجرافيك (infographic)
   - العنوان المعروض: إنفوجرافيك
   - عدد العناصر: 6
   - الترتيب: حسب الرغبة
3. احفظ
```

### 2. استخدام المكون مباشرة

```vue
<template>
  <InfographicSection
    title="إنفوجرافيك"
    subtitle="استعرض الإحصائيات والبيانات المرئية"
    :limit="6"
  />
</template>

<script setup>
import InfographicSection from '~/components/sections/InfographicSection.vue'
</script>
```

---

## 🛠️ إدارة الإنفوجرافيكات من لوحة التحكم

### الوصول
```
Admin Panel → الوسائط → إدارة الإنفوجرافيك
```

### الوظائف المتاحة

#### ✅ إضافة إنفوجرافيك جديد
1. اضغط على "إضافة إنفوجرافيك جديد"
2. املأ الحقول:
   - **العنوان (عربي)**: مطلوب
   - **العنوان (English)**: اختياري
   - **الوصف (عربي)**: اختياري
   - **الوصف (English)**: اختياري
   - **الصورة**: مطلوب (JPG, PNG, GIF, WEBP - حد أقصى 5MB)
   - **القسم**: اختياري
   - **Slug**: يُنشأ تلقائياً من العنوان
   - **الترتيب**: رقم الترتيب (الأقل أولاً)
   - **الوسوم**: افصل بفاصلة
   - **نشط**: لتفعيل/تعطيل العرض
   - **مميز**: لإضافته للقائمة المميزة
3. احفظ

#### ✏️ تعديل إنفوجرافيك
- اضغط على أيقونة القلم في جدول القائمة
- عدّل البيانات
- يمكنك تغيير الصورة أو الإبقاء على القديمة
- احفظ التغييرات

#### 🗑️ حذف إنفوجرافيك
- اضغط على أيقونة سلة المهملات
- تأكيد الحذف
- سيتم حذف الصورة تلقائياً من الخادم

#### 🔄 تغيير الحالة
- **تفعيل/تعطيل**: اضغط على زر الحالة (أخضر/رمادي)
- **مميز/غير مميز**: اضغط على أيقونة النجمة ⭐

---

## 🔍 أمثلة على الاستخدام

### مثال 1: عرض آخر 4 إنفوجرافيكات

```vue
<script setup>
const { data } = await useAsyncData('latestInfographics', async () => {
  const api = useApi()
  return await api.apiFetch('/infographics/latest?limit=4')
})
</script>

<template>
  <div v-if="data">
    <div v-for="item in data" :key="item.id">
      <img :src="getImageUrl(item.image)" :alt="item.title" />
      <h3>{{ item.title }}</h3>
    </div>
  </div>
</template>
```

### مثال 2: فلترة حسب القسم

```javascript
// Fetch infographics for specific category
const categoryId = 5
const response = await $fetch('/api/v1/infographics', {
  params: {
    category_id: categoryId,
    limit: 10
  }
})
```

---

## 🎯 السيناريوهات الشائعة

### 1. إضافة إنفوجرافيك للانتقلي السياسي

```
العنوان: التطورات السياسية في الفترة الانتقالية
الوصف: نظرة شاملة على أهم التطورات والأحداث
القسم: سياسة
الوسوم: انتقالي, سياسة, أحداث
الحالة: نشط + مميز
```

### 2. عرض إنفوجرافيكات قسم الاقتصاد فقط

```vue
<InfographicSection
  title="إنفوجرافيك الاقتصاد"
  :settings="{ category_id: 3 }"
  :limit="6"
/>
```

---

## 📋 Checklist للتحقق من التثبيت

- [ ] تم تشغيل Migration بنجاح
- [ ] الجدول `infographics` موجود في قاعدة البيانات
- [ ] الموديل `Infographic.php` يعمل بشكل صحيح
- [ ] Routes الـ API تستجيب بشكل صحيح
- [ ] صفحات لوحة التحكم تعمل
- [ ] رابط الإنفوجرافيك ظاهر في القائمة الجانبية
- [ ] المكون `InfographicSection.vue` يعرض البيانات
- [ ] نوع "infographic" متاح في أقسام الصفحة الرئيسية
- [ ] رفع الصور يعمل بشكل صحيح
- [ ] Lightbox (النافذة المنبثقة) تعمل

---

## 🐛 استكشاف الأخطاء

### المشكلة: الصور لا تظهر

**الحل:**
```bash
php artisan storage:link
```
تأكد من أن المجلد `public/storage` موجود ويشير إلى `storage/app/public`

### المشكلة: API يرجع خطأ 404

**الحل:**
- تأكد من تشغيل Migration
- تحقق من وجود Routes في `routes/api.php`
- امسح الـ cache:
```bash
php artisan route:clear
php artisan config:clear
php artisan cache:clear
```

### المشكلة: المكون لا يظهر في الفرونت إند

**الحل:**
- تأكد من إضافة نوع "infographic" في `DynamicSection.vue`
- تحقق من استيراد المكون بشكل صحيح
- افحص Console المتصفح للأخطاء

---

## 🚀 التطويرات المستقبلية المقترحة

- [ ] إضافة نظام التعليقات
- [ ] إضافة نظام التقييم (Rating)
- [ ] إمكانية مشاركة الإنفوجرافيك على وسائل التواصل
- [ ] إحصائيات متقدمة للمشاهدات
- [ ] نظام الإشعارات عند إضافة إنفوجرافيك جديد
- [ ] إمكانية تحميل الإنفوجرافيك كصورة
- [ ] Gallery mode لعرض متعدد الصور
- [ ] دعم الفيديو إنفوجرافيك

---

## 📞 الدعم

في حال واجهتك أي مشكلة:
1. راجع هذا الدليل أولاً
2. تحقق من ملفات الـ Logs في `storage/logs/laravel.log`
3. افحص Console المتصفح للأخطاء في الفرونت إند

---

## 📝 ملاحظات مهمة

⚠️ **تذكر:**
- الصور المرفوعة تُخزن في `storage/app/public/infographics/`
- الحد الأقصى لحجم الصورة: 5MB
- الصيغ المدعومة: JPG, JPEG, PNG, GIF, WEBP
- Slug يُنشأ تلقائياً من العنوان ولكن يمكن تعديله
- عند حذف إنفوجرافيك، تُحذف الصورة تلقائياً

---

## ✅ الخلاصة

تم تطوير نظام إنفوجرافيك متكامل يوفر:
- ✨ واجهة مستخدم احترافية
- 🔧 إدارة سهلة من لوحة التحكم
- 🌐 API كامل للاستخدام البرمجي
- 📱 تصميم متجاوب
- 🌍 دعم ثنائي اللغة
- ⚡ أداء محسّن

**تاريخ الإنشاء:** 21 نوفمبر 2025  
**الإصدار:** 1.0.0

---

🎉 **مبروك! النظام جاهز للاستخدام**
