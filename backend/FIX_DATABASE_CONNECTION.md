# 🔧 حل مشكلة: No connection could be made

## المشكلة
```
SQLSTATE[HY000] [2002] No connection could be made because the target machine actively refused it
```

## السبب
MySQL server غير مُشغّل في XAMPP

---

## ✅ الحل السريع

### الخطوة 1: افتح XAMPP Control Panel
1. ابحث عن "XAMPP Control Panel" في Windows
2. أو افتح: `C:\xampp\xampp-control.exe`

### الخطوة 2: شغّل MySQL
1. في XAMPP Control Panel، ابحث عن "MySQL"
2. اضغط زر **"Start"** بجانب MySQL
3. انتظر حتى يتحول إلى اللون الأخضر

### الخطوة 3: تحقق من النجاح
بعد تشغيل MySQL، جرب Migration مرة أخرى:

```bash
php artisan migrate
```

يجب أن يعمل الآن! ✅

---

## 🔍 إذا لم تنجح المحاولة السابقة

### تحقق من إعدادات .env

افتح ملف `.env` وتأكد من الإعدادات التالية:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=newsrooom
DB_USERNAME=root
DB_PASSWORD=
```

⚠️ **ملاحظة**: في XAMPP الافتراضي:
- Username: `root`
- Password: **فارغ** (لا تضع شيء)

### إنشاء قاعدة البيانات

1. افتح المتصفح واذهب إلى: http://localhost/phpmyadmin
2. اضغط "New" في القائمة اليسرى
3. اسم قاعدة البيانات: `newsrooom`
4. اضغط "Create"

### تحقق من Port

إذا كان MySQL يستخدم port مختلف:

```env
DB_PORT=3307  # بدلاً من 3306
```

---

## 🐛 استكشاف أعمق

### اختبار الاتصال بقاعدة البيانات

```bash
php artisan tinker
```

ثم:
```php
DB::connection()->getPdo();
```

إذا نجح، ستحصل على: `PDO object`  
إذا فشل، ستحصل على خطأ يوضح المشكلة

### فحص حالة MySQL

في Command Prompt:
```bash
netstat -an | findstr 3306
```

إذا لم يظهر شيء، MySQL غير مشغل.

---

## ✅ Checklist

- [ ] XAMPP Control Panel مفتوح
- [ ] MySQL مُشغّل (زر Start باللون الأخضر)
- [ ] Port 3306 يعمل
- [ ] قاعدة البيانات `newsrooom` موجودة في phpMyAdmin
- [ ] إعدادات .env صحيحة
- [ ] `php artisan migrate` يعمل بنجاح

---

## 💡 نصيحة للمستقبل

**دائماً شغّل هذين الاثنين في XAMPP قبل العمل:**
1. ✅ Apache (للـ web server)
2. ✅ MySQL (لقاعدة البيانات)

---

## 🎯 بعد حل المشكلة

بمجرد نجاح Migration، أكمل إعداد نظام الترجمة:

```bash
# 1. تشغيل Migration (يجب أن ينجح الآن)
php artisan migrate

# 2. إضافة GEMINI_API_KEY في .env
# احصل عليه من: https://makersuite.google.com/app/apikey

# 3. اختبار النظام
php test_translation.php

# 4. تشغيل Queue Worker
php artisan queue:work
```

✨ **وهكذا يكون نظام الترجمة جاهزاً!**
