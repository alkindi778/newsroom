# Google Cloud Translation API Setup

## 🎁 المميزات
- $300 رصيد مجاني لمدة 90 يوم
- 500K حرف/شهر مجاناً بعد انتهاء الفترة التجريبية
- سرعة عالية جداً
- جودة ترجمة ممتازة

## 📝 خطوات الإعداد

### 1. إنشاء حساب Google Cloud
1. اذهب إلى: https://console.cloud.google.com/
2. سجل دخول بحساب Google
3. فعّل Free Trial (يحتاج بطاقة ائتمان للتحقق فقط)

### 2. إنشاء مشروع جديد
```
- اضغط "Select a project" > "New Project"
- اسم المشروع: "Newsroom Translation"
- اضغط "Create"
```

### 3. تفعيل Cloud Translation API
```
1. من القائمة الجانبية: APIs & Services > Library
2. ابحث عن: "Cloud Translation API"
3. اضغط على النتيجة الأولى
4. اضغط "Enable"
```

### 4. إنشاء API Key
```
1. من القائمة: APIs & Services > Credentials
2. اضغط "+ Create Credentials"
3. اختر "API Key"
4. انسخ المفتاح
```

### 5. تأمين API Key (مهم!)
```
1. اضغط على القلم لتعديل API Key
2. في "API restrictions":
   - اختر "Restrict key"
   - حدد فقط: "Cloud Translation API"
3. Save
```

### 6. تحديث .env
```bash
GOOGLE_CLOUD_API_KEY=your_api_key_here
```

### 7. تحديث الكود
أضف في `config/services.php`:
```php
'google_cloud' => [
    'api_key' => env('GOOGLE_CLOUD_API_KEY'),
],
```

## 📊 مراقبة الاستخدام
- اذهب إلى: https://console.cloud.google.com/billing/
- شاهد استهلاكك من الـ $300 رصيد
- بعد 90 يوم، سينتقل تلقائياً للحد المجاني (500K/شهر)

## 💰 التكلفة
- **أول 90 يوم**: مجاني ($300 رصيد)
- **بعد ذلك**: 500K حرف/شهر مجاناً
- **إذا تجاوزت الحد**: $20 لكل مليون حرف (لن يشحن بدون موافقتك)

## ⚠️ تنبيهات
- لن يتم الشحن التلقائي - يجب أن توافق على الترقية
- يمكنك وضع حد أقصى للإنفاق في الإعدادات
- الـ 500K حرف/شهر مجاني للأبد بعد انتهاء الفترة التجريبية
