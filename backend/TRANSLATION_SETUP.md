# 🚀 دليل الإعداد السريع لنظام الترجمة

## الخطوة 1️⃣: الحصول على Gemini API Key

1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. سجل دخول بحساب Google
3. اضغط على "Create API Key"
4. انسخ المفتاح

## الخطوة 2️⃣: إضافة المفتاح في `.env`

افتح ملف `.env` وأضف:

```env
GEMINI_API_KEY=your-api-key-here
```

## الخطوة 3️⃣: تشغيل Migration

```bash
php artisan migrate
```

## الخطوة 4️⃣: تشغيل Queue Worker

في Terminal جديد، شغل:

```bash
php artisan queue:work
```

**ملاحظة**: اترك هذا Terminal مفتوحاً ليستمر في معالجة الترجمات.

## الخطوة 5️⃣: اختبار النظام

### طريقة 1: إنشاء مقال جديد
انتقل لصفحة إضافة مقال جديد وأنشئ مقالاً. ستتم الترجمة تلقائياً في الخلفية.

### طريقة 2: ترجمة المقالات الموجودة
```bash
php artisan articles:translate
```

### طريقة 3: اختبار الاتصال بـ API
```bash
php artisan tinker
```
ثم:
```php
$service = app(\App\Services\GeminiTranslationService::class);
$service->testConnection(); // يجب أن يرجع true
```

## 🔍 مراقبة الترجمة

### مشاهدة Logs
```bash
tail -f storage/logs/laravel.log
```

### فحص Jobs الفاشلة
```bash
php artisan queue:failed
```

### إعادة تشغيل Jobs الفاشلة
```bash
php artisan queue:retry all
```

## ✅ التحقق من النجاح

بعد دقائق قليلة من إنشاء مقال:

```bash
php artisan tinker
```
```php
$article = \App\Models\Article::latest()->first();
echo $article->title_en;    // يجب أن تظهر الترجمة الإنجليزية
echo $article->content_en;  // يجب أن يظهر المحتوى المترجم
```

## 🎯 نصائح مهمة

1. **Queue Worker**: تأكد دائماً من تشغيل `php artisan queue:work`
2. **API Key**: احفظه في مكان آمن ولا تشاركه
3. **Logs**: راقب الـ logs لأي أخطاء
4. **الحصص**: Gemini Flash مجاني لكن له حدود، راجع [التسعيرة](https://ai.google.dev/pricing)

## 🐛 حل المشاكل الشائعة

### "Translation job dispatched" لكن لا توجد ترجمة
**الحل**: تأكد من تشغيل Queue Worker

### "Gemini API Error"
**الحل**: 
1. تحقق من صحة API Key
2. تأكد من اتصالك بالإنترنت
3. راجع حصة الاستخدام في Google AI Studio

### الترجمة بطيئة جداً
**الحل**: 
1. شغل عدة Queue Workers متزامنة:
   ```bash
   php artisan queue:work &
   php artisan queue:work &
   php artisan queue:work &
   ```

## 📚 المزيد من المعلومات

راجع الملف الكامل: `TRANSLATION_SYSTEM.md`

---

**تم! 🎉** نظام الترجمة جاهز للعمل!
