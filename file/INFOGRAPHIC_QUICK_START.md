# 🚀 الإنفوجرافيك - دليل البدء السريع

## ⚡ التثبيت بأمر واحد (موصى به)

```bash
cd backend
php artisan infographic:setup --seed
```

هذا الأمر سيقوم تلقائياً بـ:
- ✅ تشغيل Migration
- ✅ إضافة الصلاحيات
- ✅ إضافة بيانات تجريبية (إذا استخدمت --seed)
- ✅ التحقق من Storage Link

**بدون بيانات تجريبية:**
```bash
php artisan infographic:setup
```

---

## 📝 أو التثبيت اليدوي خطوة بخطوة

### 1️⃣ تشغيل Migration
```bash
cd backend
php artisan migrate
```

### 2️⃣ إضافة الصلاحيات
```bash
php artisan db:seed --class=InfographicPermissionsSeeder
```

### 3️⃣ (اختياري) بيانات تجريبية
```bash
php artisan db:seed --class=InfographicSeeder
```

### 4️⃣ التأكد من Storage Link
```bash
php artisan storage:link
```

---

## ✅ التحقق من التثبيت

### تحقق من قاعدة البيانات
```sql
-- تحقق من الجدول
SELECT * FROM infographics LIMIT 1;

-- تحقق من الصلاحيات
SELECT * FROM permissions WHERE name LIKE '%infographic%';
```

### تحقق من لوحة التحكم
1. سجل دخول إلى لوحة التحكم: `/admin`
2. يجب أن تشاهد "إدارة الإنفوجرافيك" في القائمة الجانبية (قسم الوسائط)
3. اضغط عليه للوصول إلى صفحة الإدارة

### تحقق من API
```bash
# اختبار API
curl http://localhost:8000/api/v1/infographics
```

---

## 📋 إضافة قسم في الصفحة الرئيسية

### من لوحة التحكم:
1. اذهب إلى: **الإعدادات** → **قوالب الصفحة الرئيسية**
2. اضغط على: **إضافة قسم جديد**
3. املأ البيانات:
   - **اسم القسم**: إنفوجرافيك
   - **نوع القسم**: اختر **إنفوجرافيك** من القائمة
   - **العنوان المعروض**: إنفوجرافيك
   - **العنوان الفرعي** (اختياري): استعرض البيانات المرئية
   - **عدد العناصر**: 6
   - **الترتيب**: حسب رغبتك (مثلاً: 5)
   - **تفعيل القسم**: ✅ فعّل
4. احفظ

---

## 🎨 إضافة إنفوجرافيك جديد

### من لوحة التحكم:
1. اذهب إلى: **الوسائط** → **إدارة الإنفوجرافيك**
2. اضغط على: **إضافة إنفوجرافيك جديد**
3. املأ البيانات:
   - **العنوان (عربي)**: مطلوب
   - **العنوان (English)**: اختياري
   - **الوصف**: اختياري
   - **الصورة**: مطلوب (JPG, PNG, GIF, WEBP - حد أقصى 5MB)
   - **القسم**: اختياري
   - **الوسوم**: افصل بفاصلة (مثال: سياسة، اقتصاد)
   - **نشط**: ✅
   - **مميز**: اختياري
4. احفظ

---

## 🔐 الصلاحيات

### توزيع الصلاحيات:

| الدور | عرض | إضافة | تعديل | حذف | إدارة |
|------|-----|-------|-------|------|-------|
| **Super Admin** | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Admin** | ✅ | ✅ | ✅ | ❌ | ✅ |
| **Editor** | ✅ | ✅ | ✅ | ❌ | ❌ |
| **Reporter** | ✅ | ❌ | ❌ | ❌ | ❌ |

### لإعطاء صلاحية لمستخدم معين:
```bash
php artisan tinker
```
```php
$user = User::find(USER_ID);
$user->givePermissionTo('view_infographics');
$user->givePermissionTo('create_infographics');
$user->givePermissionTo('edit_infographics');
```

---

## 🔧 استكشاف الأخطاء السريع

### المشكلة: الصور لا تظهر
```bash
php artisan storage:link
```

### المشكلة: خطأ 403 (Forbidden)
```bash
# تأكد من تشغيل Seeder الصلاحيات
php artisan db:seed --class=InfographicPermissionsSeeder

# تحقق من صلاحيات المستخدم
php artisan tinker
User::find(YOUR_USER_ID)->permissions;
```

### المشكلة: API يرجع 404
```bash
php artisan route:clear
php artisan config:clear
php artisan cache:clear
```

### المشكلة: المكون لا يظهر في الفرونت إند
- تأكد من إضافة قسم في "قوالب الصفحة الرئيسية"
- تأكد من أن نوع القسم هو "infographic"
- افحص Console المتصفح للأخطاء

---

## 📊 API Endpoints السريعة

```bash
# قائمة الإنفوجرافيكات
GET /api/v1/infographics

# المميزة
GET /api/v1/infographics/featured

# الأحدث
GET /api/v1/infographics/latest?limit=6

# الأكثر مشاهدة
GET /api/v1/infographics/popular?limit=6

# عرض واحد
GET /api/v1/infographics/{slug}

# ذات الصلة
GET /api/v1/infographics/{slug}/related
```

---

## 📁 الملفات المهمة

### Backend:
```
app/Models/Infographic.php
app/Http/Controllers/Admin/InfographicController.php
app/Http/Controllers/Api/InfographicController.php
database/migrations/*_create_infographics_table.php
database/seeders/InfographicPermissionsSeeder.php
routes/api.php (محدث)
routes/admin.php (محدث)
```

### Frontend:
```
components/sections/InfographicSection.vue
components/DynamicSection.vue (محدث)
```

---

## 🎯 المسارات المهمة

- **لوحة التحكم**: `/admin/infographics`
- **إضافة جديد**: `/admin/infographics/create`
- **API الرئيسي**: `/api/v1/infographics`

---

## 📞 مساعدة إضافية

للمزيد من التفاصيل، راجع:
```
📄 INFOGRAPHIC_SYSTEM_README.md
```

---

## ✅ Checklist سريع

قبل الانتهاء، تأكد من:

- [ ] ✅ تم تشغيل Migration
- [ ] ✅ تم تشغيل Permissions Seeder
- [ ] ✅ رابط الإنفوجرافيك ظاهر في القائمة الجانبية
- [ ] ✅ يمكن إضافة إنفوجرافيك جديد
- [ ] ✅ الصور تُرفع وتُعرض بشكل صحيح
- [ ] ✅ API يعمل ويرجع البيانات
- [ ] ✅ المكون يظهر في الفرونت إند
- [ ] ✅ نوع "infographic" متاح في أقسام الصفحة الرئيسية

---

**🎉 مبروك! نظام الإنفوجرافيك جاهز للاستخدام**

**التاريخ:** 21 نوفمبر 2025  
**الإصدار:** 1.0.0
