# خطوات دمج قاعدة البيانات القديمة - الدليل المبسط

## الوضع الحالي ✅
- ✅ قاعدة البيانات القديمة `adenstc_db` مستوردة في phpMyAdmin
- ✅ إعدادات الاتصال موجودة في ملف `.env`
- ✅ أمر Laravel للدمج جاهز

## ما سيتم نقله (بدون بيانات المستخدمين) 📋

### ✅ سيتم النقل:
1. **الكتّاب** (writers) → من جدول `writers` القديم
   - الاسم، البريد، السيرة الذاتية، الصورة
   - روابط فيسبوك وتويتر
   
2. **مقالات الرأي** (opinions) → من جدول `articles` القديم
   - العنوان، المحتوى، الصورة
   - التاريخ، عدد المشاهدات
   - المقالات المميزة
   
3. **الأخبار** (articles) → من جدول `posts` القديم
   - العنوان، المحتوى، الصورة
   - التاريخ، عدد المشاهدات، المصدر
   - الأخبار المميزة

### ❌ لن يتم نقله:
- ❌ جدول المستخدمين (users)
- ❌ أسماء المستخدمين وكلمات المرور
- ❌ صلاحيات المستخدمين

---

## خطوات التنفيذ 🚀

### الخطوة 1️⃣: التحقق من التوصيل
افتح Terminal/CMD في مجلد المشروع وتأكد من أن XAMPP يعمل:

```cmd
cd c:\xampp\htdocs\newsroom\backend
php artisan tinker
```

إذا فتح tinker بنجاح، اكتب:
```php
DB::connection('old_database')->table('posts')->count()
exit
```

إذا ظهر رقم، فالاتصال بقاعدة البيانات القديمة يعمل ✅

---

### الخطوة 2️⃣: نسخ الصور القديمة (إذا كانت موجودة)
إذا كان لديك مجلد الصور القديم:

```cmd
# أنشئ مجلد للصور القديمة
mkdir backend\storage\app\public\old_photos

# انسخ الصور القديمة إلى المجلد الجديد
# من مجلد photos القديم إلى backend\storage\app\public\old_photos
```

> **ملاحظة:** إذا لم يكن لديك الصور، لا مشكلة، سيتم تخطي الصور المفقودة تلقائياً.

---

### الخطوة 3️⃣: تنفيذ الدمج

**لدمج كل شيء دفعة واحدة:**
```cmd
cd c:\xampp\htdocs\newsroom\backend
php artisan migrate:old-database
```

**أو خطوة بخطوة:**

```cmd
# 1. دمج الكتاب أولاً
php artisan migrate:old-database --step=writers

# 2. دمج مقالات الرأي
php artisan migrate:old-database --step=opinions

# 3. دمج الأخبار
php artisan migrate:old-database --step=articles
```

---

### الخطوة 4️⃣: التحقق من النتائج

بعد اكتمال الدمج، تحقق من عدد السجلات المنقولة:

```cmd
php artisan tinker
```

ثم نفذ:
```php
// عدد الكتاب
App\Models\Writer::count()

// عدد مقالات الرأي
App\Models\Opinion::count()

// عدد الأخبار
App\Models\Article::count()

exit
```

---

## حل المشاكل المحتملة 🔧

### ❌ مشكلة: Connection refused
**الحل:**
- تأكد من تشغيل XAMPP (Apache + MySQL)
- تحقق من إعدادات الاتصال في `.env`

### ❌ مشكلة: Table 'adenstc_db.posts' doesn't exist
**الحل:**
- تأكد من استيراد قاعدة البيانات القديمة بنجاح في phpMyAdmin
- اسم قاعدة البيانات يجب أن يكون `adenstc_db` بالضبط

### ❌ مشكلة: Duplicate entry
**الحل:**
- إذا كررت عملية الدمج، ستحصل على خطأ التكرار
- احذف البيانات المكررة يدوياً من قاعدة البيانات الجديدة أولاً

---

## التنظيف بعد الدمج 🧹

بعد التأكد من نجاح الدمج وعمل كل شيء:

1. **يمكنك حذف قاعدة البيانات القديمة من phpMyAdmin:**
   - افتح phpMyAdmin
   - اختر قاعدة البيانات `adenstc_db`
   - اضغط على Drop

2. **احذف ملف SQL القديم إذا لم تعد بحاجة إليه:**
   ```cmd
   # ملف SQL القديم موجود في:
   # c:\xampp\htdocs\newsroom\adenstc_db.sql\adenstc_db.sql
   ```

---

## ملاحظات مهمة 📌

1. **الصور:** مسارات الصور ستتحول من `/photos/` إلى `/storage/old_photos/`
2. **الفئات:** المقالات بدون فئات ستذهب إلى فئة "عام" الافتراضية
3. **المستخدمين:** المقالات ستُنسب إلى المستخدم الافتراضي (admin@example.com)
4. **الكتاب:** إذا لم يُعثر على كاتب، سيتم إنشاء "كاتب افتراضي"

---

## الأمان 🔒

✅ **لن يتم نقل:**
- كلمات مرور المستخدمين
- جلسات المستخدمين
- رموز المصادقة
- معلومات تسجيل الدخول

فقط البيانات العامة (المقالات، الأخبار، الكتاب) سيتم نقلها.

---

## في حال الحاجة للمساعدة 💬

إذا واجهت مشاكل:
1. راجع سجلات Laravel: `backend\storage\logs\laravel.log`
2. راجع رسائل الخطأ في Terminal
3. تأكد من صحة البيانات في القاعدة القديمة
